{
  "name": "samkovai-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend --",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend --",
    "typecheck": "npm run typecheck --workspace frontend",
    "test": "node scripts/test-backend.cjs",
    "test:frontend": "node scripts/test-frontend.cjs",
    "test:database": "node scripts/test-database.cjs",
    "check": "npm run check --workspace frontend"
  },
  "overrides": {
    "postcss": "^8.5.23"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
